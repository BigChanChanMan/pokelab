# POKÉLAB 上下文

训练家工作台 —— 一个用能力模型承载权限系统的宝可梦对战工具。这份文件只记录领域词汇，不含实现细节。

## 身份（Identity）

**Trainer（训练家）**
这个平台上的**账号**。有训练家名、密码、等级。
说「训练家」指的是账号，说「宝可梦」指的是被构筑的对象 —— 两者绝不混用。
_Avoid_: 用户、账号、player

**训练家名（Trainer name）**
训练家的登录凭据**兼**展示名。唯一，不可改。
_规则_：登录名和展示名是**同一个字段**。要拆开时再拆，那时才需要回答「改了名还能不能登录」。
_Avoid_: 用户名、昵称、handle

**等级（Tier）**
训练家的权限层级。只有四档，从低到高：访客 / 注册训练家 / VIP / 管理员。
_规则_：**等级是阶梯，不是集合** —— 高一档拥有低一档的全部能力。
_Avoid_: 角色、权限组、userType

**访客（Guest）**
**未登录**的访问者。是一个隐式的训练家，等级最低。
_规则_：访客不是一档「可以被设成」的等级。管理员最低只能把人设到注册训练家 ——
「有没有会话」和「什么等级」是两件事，不能用一个字段承担。
_Avoid_: 匿名用户、游客

**管理员（Admin）**
最高等级。拥有 VIP 的全部能力，外加管理用户与升级码。
_规则_：**只能由 seed 产生，界面不可授予**（因为「谁能成为管理员」必须有一条唯一、
显式的路径）；但**可以被降级**，否则它就是一个无法撤销的权限。
_Avoid_: 超管、root、运营

**能力（Capability）**
一个**具体的、可执行的动作**，例如「队伍诊断」。
_规则_：等级是**人的属性**，能力是**动作的属性**，两者通过一张映射表连接。全项目只有一处判定。
_Avoid_: 权限、feature、菜单项

**升级码（Upgrade code）**
一段文本，输入它可以把训练家的等级升到 VIP。全局唯一、可重复使用，由管理员设置。
_规则_：升级码只回答「能不能升级」；「谁升级了」由训练家自己的等级记录回答。
_Avoid_: 邀请码、兑换码、激活码

## 队伍（Team）

**Team（队伍）**
一个训练家名下的宝可梦成员集合，最多 6 名，有名字。
_Avoid_: 小队、战队

**Member（成员）**
队伍里的一只宝可梦**加上它的配置**。一支队伍里同一物种最多一只。
_Avoid_: 宝可梦、精灵

**队伍名（Team name）**
队伍的可编辑展示名。非空、最多 30 字；新建与重命名共用同一套约束，服务端强制。允许重名。
_Avoid_: 队名、标题

## 抽卡（DailyDraw）

**稀有度（Rarity）**
一张**卡牌**的稀有层级，五档：`N` / `R` / `SR` / `SSR` / `UR`。
_规则_：**`Tier`（等级）永久专属训练家，`Rarity`（稀有度）永久专属卡牌**，两者绝不复用同一个词。
它们都是「有序的档位轴」，都容易写成 `TIER_RANK` —— 所以这条规则必须显式，否则两份 rank 表会在同一个
文件里撞名。代码里一律 `CardRarity` / `RARITY_RANK` / `RARITY_LABEL`，中文一律说「稀有度」。
_Avoid_: 档位、tier、等级

**抽卡（Draw）**
一次每日抽取的结果：**日期 + 卡牌**。
_规则_：结果是**日期的纯函数** —— 同一天、任何训练家、任何设备，抽到的是同一张卡。
服务端实现**不得**把训练家 id 混进随机种子，否则「全球同卡」立刻失效。
所以抽卡结果本身**不可作弊**，抽卡册才是需要服务端强制的资产。
_Avoid_: 开包、roll

**抽卡册（Album）**
一个训练家名下历次抽卡结果的集合。
_规则_：**VIP 降级之后册子仍然看得见。** 里面的东西是训练家的资产，
等级只影响「能不能再抽」，不影响「能不能看」（DESIGN.md §6.4「降级不删数据」）。
这也是能力拆成 `gacha.draw`（VIP）和 `gacha.album`（注册即可）两个的原因。
_Avoid_: 卡册、收藏、图鉴（图鉴是宝可梦图鉴，另一回事）

**保底（Pity）**
连续若干天未获得某档稀有度时，强制提升该次抽取档位的机制。
_规则_：保底计数**同样只由日期决定**，因此全球所有训练家的保底进度天然一致。
_规则 2_：计数必须用**实际抽到的**档位推进，不能用不含保底的基础档位 ——
后者会让一次保底赠送的高档位永远不重置计数，于是「连续 N 天没抽到 X」一旦成立
就永远成立，从此每天都强制 X。见 `lib/gacha.test.ts` 的回归测试。
_Avoid_: 幸运值、垫刀