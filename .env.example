# 会话 cookie 的签名密钥。32 字节以上的随机串。
# 生成：node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
#
# 开发环境不设也能跑（会用固定兜底值并打印警告）；
# **生产环境不设会直接启动失败** —— 带着可预测的密钥上线比不启动更糟。
SESSION_SECRET=

# SQLite 文件路径。相对路径即可。
DATABASE_URL=./local.db
